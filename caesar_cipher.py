def caesar_cipher(text, shift): result = "" for char in text: if char.isalpha(): # Check if it's a letter start = ord('A') if char.isupper() else ord('a') result += chr((ord(char) - start + shift) % 26 + start) else: result += char # Keep spaces and punctuation unchanged return result # Testing the function message = input("Enter a message to encrypt: ") # This line asks for the message shift_value = int(input("Enter shift value: ")) # This line asks for the shift value encrypted_message = caesar_cipher(message, shift_value) print("Encrypted message:", encrypted_message)